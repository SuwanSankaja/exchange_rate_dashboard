{
  "name": "exchange-rate-dashboard",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=exchange-rate-dashboard"
  },
  "dependencies": {
    "mongodb": "^6.17.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}